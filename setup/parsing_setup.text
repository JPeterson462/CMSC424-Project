The following python modules are used:

- Pillow (pip install Pillow)
- Mutagen (pip install mutagen)
- HTML Parser (pip install HTMLParser)
- LXML (pip install lxml)
- Requests (pip install requests)